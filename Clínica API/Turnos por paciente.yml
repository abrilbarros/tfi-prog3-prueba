info:
  name: Turnos por paciente
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/turnos/medico/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
