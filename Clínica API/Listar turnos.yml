info:
  name: Listar turnos
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/turnos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
